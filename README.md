# python-word-indexer
A Python program that recursively scans a directory, extracts words from all files, and builds an index of word occurrences. For each word, the program stores total frequency, filenames where it appears, and exact positions by line and column. The output is sorted alphabetically by words and filenames.
